<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/action_speed"
        android:title="@string/default_animation_speed"
        android:icon="@drawable/ic_action_fast_forward"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_share"
        android:title="@string/action_share_graph"
        android:icon="@drawable/ic_share"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_clear"
        android:title="@string/action_clear_graph"
        app:showAsAction="never"/>

    <item android:id="@+id/action_export"
        android:title="@string/action_export_graph"
        app:showAsAction="never"/>

    <item android:id="@+id/action_is_directed"
        android:title="@string/is_directed"
        app:showAsAction="never"
        android:checkable="true"
        android:checked="true"/>

</menu>
